[project]
name = "cachespeech"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Angad", email = "angadsankhla09@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
cachespeech = "cachespeech:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
